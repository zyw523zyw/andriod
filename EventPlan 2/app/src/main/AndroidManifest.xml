<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="assignment1.eventplan">

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="{AIzaSyByuI-5l6vXmqFBBo1efyLxlgA5K87p57c}"/>

        <service android:name=".services.ContactBackupService" />

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.AddOrUpdateEventActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PlacePickerActivity"/>
        <activity android:name=".activity.ContactsPickerActivity"/>
        <activity android:name=".activity.DetailActivity" />
        <activity android:name=".datepicker" />
        <activity android:name=".activity.CalendarActivity" />
        <activity android:name=".activity.Contacts">
            <intent-filter>
                <action android:name="android.intent.action.Contacts" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>